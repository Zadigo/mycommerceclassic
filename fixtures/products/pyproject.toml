[project]
name = "products"
version = "0.1.0"
description = "A FastAPI application for managing products, including product creation, retrieval, and image handling."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "faker>=40.37.0",
    "fastapi[standard]>=0.141.1",
    "httpx2>=2.12.0",
    "orjson>=3.12.0",
    "pillow>=12.3.0",
    "redis>=8.1.0",
]

[dependency-groups]
dev = [
    "pandas>=3.0.5",
    "pydantic>=2.13.5",
    "pytest>=9.1.1",
    "pytest-aio>=2.1.7",
    "pytest-cov>=7.1.0",
    "pytest-timeout>=2.4.0",
    "ruff>=0.16.5",
]
